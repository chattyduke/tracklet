"""report — overlay.png + report.md (S6).

Contract: write_report(score, overlay_inputs, out_dir). Reads truth only via the score result.
"""
from __future__ import annotations


def write_report(score, overlay_inputs, out_dir: str) -> None:
    raise NotImplementedError("report.write_report lands in Sprint 6")
